{
  "mcpServers": {
    "math-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "http://localhost:8008/mcp",
        "--transport",
        "http-only",
        "--allow-http"
      ]
    }
  }
}
